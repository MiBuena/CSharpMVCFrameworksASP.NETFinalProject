@model ICollection<LeisureTimeSystem.Models.ViewModels.CategoryViewModel>

<ul class="dropdown-menu">
    @foreach (var subcategory in this.Model)
    {
        <li><a href="#">@subcategory.Name</a></li>
    }
</ul>