@model LeisureTimeSystem.Models.ViewModels.Organization.RemoveRepresentativeViewModel

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RemoveRepresentativeViewModel</h4>
    <hr />
    <dl class="dl-horizontal">
     
        <dd>
            @Html.DisplayFor(model => model.RepresentativeName)
        </dd>


    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()



        @Html.Hidden("RepresentativeId", this.Model.RepresentativeId)

        @Html.Hidden("OrganizationId", this.Model.OrganizationId)

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
